# Charles Blog 上线计划（海外版 · 精简）

> 面向：主要给海外的人看你的博客。
> 结论先给：**代码放 GitHub，网站用 Netlify 部署。** 两步就完事。

---

## 一、先明白你现在有什么

你的博客是**纯静态网站**：

- 只有 HTML / CSS / JS 和图片，没有服务器，没有数据库
- 没有点赞、浏览量、留言（这些功能已经全部去掉了）
- 不需要登录，不需要后端，不存任何用户数据

这件事对你意味着三件好事：

1. **部署极其简单** —— 任何静态托管平台都能跑，基本都是「拖进去就好了」
2. **永远免费** —— 你的流量级别根本用不到付费套餐
3. **速度快** —— 纯静态文件走 CDN，海外访问几乎是秒开

**唯一的限制**：你不能在网站上「发布」新内容（因为没有后台）。想加一篇文章，就是改文件 + 推送到 GitHub，网站自动更新。这正好就是你原本的工作方式，没有损失。

---

## 二、整体方案（一张图看懂）

```
你电脑上的文件  ──git push──▶  GitHub 仓库（存代码）
                                    │
                                    │ 自动同步
                                    ▼
                            Netlify（托管网站）
                                    │
                                    ▼
                          你的网址，全世界可访问
```

- **GitHub** 只负责「存代码 + 记录每一次修改」
- **Netlify** 负责「把代码变成能打开的网站」
- 你只要往 GitHub 推代码，Netlify 会在 **1 分钟内自动重新部署**

**为什么不直接用 GitHub Pages？**
GitHub Pages 也能托管，但它在中国大陆访问很不稳定（经常打不开）。你既然主要给海外的人看，Netlify 更快、更稳，还自带免费 HTTPS、免费域名、自动部署，体验明显更好。GitHub Pages 可以作为备用，没必要一开始折腾两个。

---

## 三、第一步：把代码传到 GitHub

### 3.1 装 Git

打开 https://git-scm.com/downloads 下载安装，一路点「下一步」即可。
装完打开终端（Windows 用 Git Bash），输入：

```bash
git --version
```

能显示版本号就成功了。

### 3.2 注册 GitHub 并建仓库

1. 打开 https://github.com 注册账号（记住用户名）
2. 右上角 `+` → **New repository**
3. 填写：
   - **Repository name**：`charles-blog`
   - **Description**：随便写，比如 `My portfolio blog`
   - **Public / Private**：选 **Public**（免费账号用 Netlify 部署时，Public 最省事）
   - **不要**勾选 "Add a README file"
4. 点 **Create repository**

### 3.3 把本地文件推上去

在项目文件夹里打开终端（Windows：在文件夹空白处右键 → Git Bash Here），依次执行：

```bash
# 1. 初始化 Git 仓库
git init

# 2. 告诉 Git 你是谁（只做一次，换成你的信息）
git config user.name "你的名字"
git config user.email "你的邮箱@example.com"

# 3. 先看有哪些文件会被上传（重要！确认没有不该传的东西）
git status
```

确认没问题后：

```bash
# 4. 添加所有文件
git add .

# 5. 提交
git commit -m "第一次提交：Charles Blog 上线"

# 6. 关联远程仓库（把 URL 换成你自己的仓库地址）
git branch -M main
git remote add origin https://github.com/你的用户名/charles-blog.git

# 7. 推送
git push -u origin main
```

**第 7 步会弹出登录框**。GitHub 现在不接受密码推送，需要用 **Personal Access Token**：

1. GitHub 右上角头像 → **Settings** → 左下角 **Developer settings**
2. **Personal access tokens** → **Tokens (classic)** → **Generate new token (classic)**
3. Note 随便填，Expiration 选 `90 days` 或 `No expiration`
4. 勾选权限：**`repo`**（整个大类勾上就够）
5. 点 **Generate token**，**立刻复制那串字符**（页面关掉就再也看不到了）
6. 回到终端，密码位置**粘贴这串 token**（不是你的登录密码）

推送成功后会看到 `main -> main` 的字样。

### 3.4 上传前必须处理：`.gitignore`

在项目根目录新建一个名为 `.gitignore` 的文件（注意最前面有个点），内容：

```gitignore
# 本地开发与测试工具
.workbuddy/
node_modules/

# macOS / Windows 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 编辑器配置
.vscode/
.idea/

# 日志
*.log
```

**为什么要这个？** `.workbuddy/` 里放的是开发时的临时脚本、调试截图、浏览器缓存，体积可能有几十 MB，而且跟网站本身无关。传上去只会拖慢仓库，还可能泄露你的本地路径信息。

### 3.5 推送前自检清单

- [ ] 网站能正常运行（本地打开 `index.html` 或起个本地服务看看）
- [ ] 点赞、浏览量、留言这些功能已经全部移除，页面上看不到它们
- [ ] 没有把 `.workbuddy/` 目录传上去
- [ ] 图片文件都在 `assets/` 下面，没有缺图

---

## 四、第二步：让网站能公开访问（Netlify）

### 4.1 部署

1. 打开 https://www.netlify.com，点 **Sign up**
2. 选择 **Sign up with GitHub**（用 GitHub 账号直接注册，省得再记一套密码）
3. 授权 Netlify 访问你的 GitHub
4. 进入后台，点 **Add new site** → **Import an existing project**
5. 选择 **GitHub**，然后选刚才建的 `charles-blog` 仓库
6. 接下来的构建配置：
   - **Build command**：**留空**
   - **Publish directory**：填 `.`（就是一个点，表示根目录）
   - 其他全部保持默认
7. 点 **Deploy site**

**等大约 30 秒到 1 分钟**，页面会显示 `Published`，并给你一个网址，形如：

```
https://random-name-123456.netlify.app
```

**这个网址就是你的博客，现在全世界都能打开了。**

### 4.2 把网址改好看一点（可选）

默认网址是随机字符串，很难看。改掉：

1. 在站点页面点 **Site configuration** → **Change site name**
2. 输入你喜欢的名字，比如 `charles-blog`
3. 网址就变成 `https://charles-blog.netlify.app`

### 4.3 验证是否成功

- 用手机流量（不要用 Wi-Fi）打开这个网址，能打开就说明外网可访问
- 点进几个科目页，再点进一篇文章，确认所有页面都正常
- 网址左边应该有个**小锁图标**（HTTPS 已自动配好，不用你管）

---

## 五、第三步：绑定自己的域名（可选，但强烈推荐）

`.netlify.app` 的网址发给别人显得不够正式。买个域名大概 **10–15 美元/年**，一劳永逸。

### 5.1 买域名

推荐 **Cloudflare Registrar**（按成本价卖，不加价）或 **Namecheap**。
域名建议：`你的名字.com` 或 `你的名字.blog`。

### 5.2 绑定

1. Netlify 后台 → **Domain management** → **Add a domain**
2. 输入你买的域名，点 **Verify** → **Add domain**
3. Netlify 会告诉你需要配置哪些 DNS 记录（通常是两条）
4. 回到域名商后台，把这两条记录填进去
5. 等几分钟到几小时，DNS 生效后域名就指向你的博客了

**切记**：Netlify 会自动为你的域名申请免费的 HTTPS 证书，不用另外买。

---

## 六、以后怎么更新博客

### 6.1 日常更新流程

改完文件后，在项目目录执行三条命令：

```bash
git add .
git commit -m "新增 BCM212 第二篇作业"
git push
```

推完等 1 分钟左右，Netlify 会**自动重新部署**，网站就变成新版了。你不需要做任何其他操作。

### 6.2 写提交信息的习惯

`git commit -m` 后面的文字是给未来的自己看的。写得具体一点：

- 好：`新增 BCM241 田野笔记文章`
- 好：`修复手机上导航栏重叠`
- 差：`更新`、`改了一下`

### 6.3 万一改坏了怎么办

Git 记录了你每一次的改动，随时可以回退：

```bash
# 看看历史提交
git log --oneline

# 撤回上一次提交（保留文件改动）
git reset --soft HEAD~1

# 或者直接回到某个版本（把 abc1234 换成实际的提交编号）
git checkout abc1234 -- .
```

最省事的办法：**改坏了就把文件改回来，重新 push 一次**。网站总是跟着 GitHub 上最新的代码走。

### 6.4 在手机上改

- **简单改文字**：直接在 GitHub 网页版打开文件 → 点铅笔图标编辑 → 提交，Netlify 会自动更新
- **上传新图片**：GitHub 网页版支持直接拖拽上传

---

## 七、费用总结

| 项目 | 费用 |
|---|---|
| GitHub 账号 + 仓库 | 免费 |
| Netlify 托管（个人站点） | 免费（每月 100GB 流量，你绝对用不完） |
| HTTPS 证书 | 免费（自动） |
| 域名（可选） | 约 10–15 美元/年 |

**除了域名全是免费的。** 不买域名的话，总花费是 **0 元**。

---

## 八、踩坑清单

- **推送要 token，不要密码**：GitHub 早就禁用密码推送了，报 `Authentication failed` 就是这个原因
- **token 页面关掉就看不到了**：生成后立刻复制粘贴到记事本存好
- **别把 `.workbuddy/` 推上去**：先写好 `.gitignore` 再 `git add .`
- **Netlify 的 Publish directory 填 `.`**：填错会部署出一个空白页
- **改了文件但网站没变**：先确认 `git push` 成功了（终端有没有报错），再看 Netlify 后台的 Deploys 页面有没有在跑
- **图片不显示**：检查路径大小写。Netlify（Linux 服务器）区分大小写，你本地（Windows）不区分，`Photo.PNG` 和 `photo.png` 在本地都能开，传上去就 404 了

---

## 九、一句话总结

> **装 Git → 建 GitHub 仓库 → `git push` → Netlify 连上仓库，网站就上线了。以后更新就是改文件再 `git push`，网站自动变。买个域名绑上，总花费 0 到 15 美元一年。**
